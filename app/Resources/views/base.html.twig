<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Find Code Usage{% endblock %} - code-usage.com</title>

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

      {% stylesheets filter='cssrewrite'
        'assets/bootstrap/css/bootstrap.min.css'
        'assets/font-awesome/css/font-awesome.min.css'
        'assets/spaces.css'
        'assets/style.css'
        %}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet" media="screen"/>
      {% endstylesheets %}

      {% block custom_stylesheets %}{% endblock %}
    </head>
    <body>

    <header id="top" class="navbar navbar-static-top navbar-custom clearfix">
      <div class="container-fluid">
        <div class="navbar-header">
          <button class="navbar-toggle" data-target=".bs-navbar-collapse" data-toggle="collapse" type="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('espend_inspector_frontend_home') }}"><i class="fa fa-code"></i> code-usage<span class="text-muted">.com</span></a>
        </div>


        <form class="navbar-form navbar-right pull-right navbar-form-search" role="form" method="get" action="{{ path('espend_inspector_frontend_home_post') }}">
          <div class="form-group">
            <input name="q" class="form-control q-input" type="text" placeholder="Class, Method, ...">
          </div>
          <button class="btn btn-primary" type="submit"><i class="fa fa-search"></i></button>
        </form>

      </div>
    </header>

        {% block body %}
          <div class="container-fluid margin-top30" >

            <div class="well well-white">
              {% block content %}
              {% endblock %}
            </div>

          </div>
        {% endblock %}

        {% javascripts
        'assets/jquery-2.1.1.min.js'
        'assets/js/app.js'
        %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}

        {% block javascripts %}
        {% endblock %}

    <footer class="text-center">
      Daniel Espendiller - <a href="http://espend.de">espend.de</a>
    </footer>

    </body>

</html>
